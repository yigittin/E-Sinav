using Volo.Abp.Settings;

namespace SanalVaka.Settings;

public class SanalVakaSettingDefinitionProvider : SettingDefinitionProvider
{
    public override void Define(ISettingDefinitionContext context)
    {
        //Define your own settings here. Example:
        //context.Add(new SettingDefinition(SanalVakaSettings.MySetting1));
    }
}
